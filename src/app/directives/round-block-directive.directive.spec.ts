import { RoundBlockDirectiveDirective } from './round-block-directive.directive';

describe('RoundBlockDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundBlockDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
